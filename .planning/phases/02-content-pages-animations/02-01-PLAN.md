---
phase: 02-content-pages-animations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app/layout.tsx
  - app/template.tsx
  - app/page.tsx
  - app/about/page.tsx
  - app/gallery/page.tsx
  - app/contact/page.tsx
  - app/globals.css
  - components/animation/PageTransition.tsx
  - components/animation/ScrollReveal.tsx
  - components/animation/HoverEffect.tsx
  - components/animation/LoadingSkeleton.tsx
  - components/cursor/CustomCursor.tsx
  - components/layout/SmoothScroll.tsx
  - components/layout/Navigation.tsx
  - components/layout/Footer.tsx
  - lib/animations.ts
  - lib/data/portfolio.ts
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03
  - PAGE-04
  - PAGE-05
  - ANIM-01
  - ANIM-02
  - ANIM-03
  - ANIM-04
  - ANIM-05
  - ANIM-06
  - CONTENT-01
  - CONTENT-02
  - PERF-01
  - PERF-02
user_setup: []
must_haves:
  truths:
    - "All 4 pages render with real content and placeholder images"
    - "Page transitions animate between routes"
    - "Scroll-triggered reveals fire on viewport entry"
    - "Hover effects scale and glow on interactive elements"
    - "Lenis smooth scrolling is active"
    - "Custom cursor follows mouse on desktop, hidden on touch"
    - "Loading skeletons display while content loads"
    - "prefers-reduced-motion disables all animations"
  artifacts:
    - "Framer Motion installed and configured with LazyMotion"
    - "Lenis installed and wrapping app in layout"
    - "AnimatePresence page transitions in template.tsx"
    - "Custom cursor component with mix-blend-mode"
    - "ScrollReveal and HoverEffect animation wrappers"
    - "Skeleton loading component with shimmer animation"
    - "Updated portfolio data with richer content"
  key_links:
    - "template.tsx wraps children with AnimatePresence keyed on pathname"
    - "SmoothScroll wraps app body with Lenis"
    - "LazyMotion in layout.tsx provides domAnimation features"
    - "CustomCursor in layout.tsx renders globally"
    - "portfolio.ts provides all page content data"
---

<objective>
Enrich all 4 portfolio pages with real content and placeholder images, add Framer Motion page transitions, scroll-triggered reveals, hover effects, smooth scrolling with Lenis, custom cursor effects, and loading skeletons. The result is a visually rich, animated portfolio with smooth interactions.

Purpose: Transform the Phase 1 scaffolding into a polished, interactive portfolio experience with Frutiger cyber aesthetic.

Output: A fully animated portfolio where all pages have content, transitions work, scroll reveals fire, hover effects respond, and the cursor follows with blend-mode difference.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-pages-animations/CONTEXT.md
@.planning/phases/02-content-pages-animations/RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-scaffolding-design-system/01-01-SUMMARY.md
</context>

<tasks>

<task type="quick">
  <name>Task 1: Install Animation Dependencies</name>
  <files>
    package.json
  </files>
  <action>
Per ANIM-01, ANIM-04, PERF-02:

**Step 1: Install Framer Motion and Lenis**

```bash
cd D:/goat && npm install framer-motion lenis
```

**Step 2: Verify installation**

```bash
cat package.json | grep -E "framer-motion|lenis"
```

Expected output should show both packages.

**Step 3: Commit**

```bash
git add package.json package-lock.json
git commit -m "deps: install framer-motion and lenis for animations"
```
  </action>
  <verify>
    <automated>cd D:/goat && cat package.json | grep -c "framer-motion\|lenis"</automated>
  </verify>
  <acceptance_criteria>
    - PERF-02: Framer Motion installed for lazy motion
    - ANIM-04: Lenis installed for smooth scrolling
  </acceptance_criteria>
</task>

<task type="quick">
  <name>Task 2: Create Animation Infrastructure Components</name>
  <files>
    components/animation/PageTransition.tsx,
    components/animation/ScrollReveal.tsx,
    components/animation/HoverEffect.tsx,
    components/animation/LoadingSkeleton.tsx,
    components/cursor/CustomCursor.tsx,
    components/layout/SmoothScroll.tsx,
    app/template.tsx,
    app/layout.tsx,
    app/globals.css
  </files>
  <action>
Per ANIM-01, ANIM-02, ANIM-03, ANIM-04, ANIM-05, ANIM-06, PERF-02:

**Step 1: Create PageTransition component**

Create `components/animation/PageTransition.tsx`:

```tsx
"use client";

import { motion } from "framer-motion";
import { ReactNode } from "react";

interface PageTransitionProps {
  children: ReactNode;
}

export default function PageTransition({ children }: PageTransitionProps) {
  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      exit={{ opacity: 0, y: -20 }}
      transition={{ duration: 0.3, ease: "easeInOut" }}
    >
      {children}
    </motion.div>
  );
}
```

**Step 2: Create ScrollReveal component**

Create `components/animation/ScrollReveal.tsx`:

```tsx
"use client";

import { motion } from "framer-motion";
import { ReactNode } from "react";

interface ScrollRevealProps {
  children: ReactNode;
  className?: string;
  delay?: number;
  direction?: "up" | "down" | "left" | "right";
}

const directionMap = {
  up: { y: 30 },
  down: { y: -30 },
  left: { x: 30 },
  right: { x: -30 },
};

export default function ScrollReveal({
  children,
  className = "",
  delay = 0,
  direction = "up",
}: ScrollRevealProps) {
  return (
    <motion.div
      className={className}
      initial={{ opacity: 0, ...directionMap[direction] }}
      whileInView={{ opacity: 1, x: 0, y: 0 }}
      viewport={{ once: true, margin: "-100px" }}
      transition={{ duration: 0.6, delay, ease: "easeOut" }}
    >
      {children}
    </motion.div>
  );
}
```

**Step 3: Create HoverEffect component**

Create `components/animation/HoverEffect.tsx`:

```tsx
"use client";

import { motion } from "framer-motion";
import { ReactNode } from "react";

interface HoverEffectProps {
  children: ReactNode;
  className?: string;
  scale?: number;
  glow?: boolean;
}

export default function HoverEffect({
  children,
  className = "",
  scale = 1.05,
  glow = false,
}: HoverEffectProps) {
  return (
    <motion.div
      className={`${className} ${glow ? "hover:shadow-[var(--shadow-glow-primary)]" : ""}`}
      whileHover={{ scale, y: -4 }}
      whileTap={{ scale: 0.98 }}
      transition={{ type: "spring", stiffness: 400, damping: 17 }}
    >
      {children}
    </motion.div>
  );
}
```

**Step 4: Create LoadingSkeleton component**

Create `components/animation/LoadingSkeleton.tsx`:

```tsx
interface LoadingSkeletonProps {
  className?: string;
  width?: string;
  height?: string;
}

export default function LoadingSkeleton({
  className = "",
  width = "100%",
  height = "20px",
}: LoadingSkeletonProps) {
  return (
    <div
      className={`skeleton ${className}`}
      style={{ width, height }}
      aria-hidden="true"
    />
  );
}
```

**Step 5: Create CustomCursor component**

Create `components/cursor/CustomCursor.tsx`:

```tsx
"use client";

import { useEffect, useState } from "react";

export default function CustomCursor() {
  const [position, setPosition] = useState({ x: 0, y: 0 });
  const [isHovering, setIsHovering] = useState(false);
  const [isVisible, setIsVisible] = useState(false);

  useEffect(() => {
    // Only show on non-touch devices
    const isTouchDevice = window.matchMedia("(pointer: coarse)").matches;
    if (isTouchDevice) return;

    setIsVisible(true);

    const handleMouseMove = (e: MouseEvent) => {
      setPosition({ x: e.clientX, y: e.clientY });
    };

    const handleMouseEnter = () => setIsHovering(true);
    const handleMouseLeave = () => setIsHovering(false);

    document.addEventListener("mousemove", handleMouseMove);

    // Add hover listeners to interactive elements
    const addHoverListeners = () => {
      const interactiveElements = document.querySelectorAll("a, button, [role='button']");
      interactiveElements.forEach((el) => {
        el.addEventListener("mouseenter", handleMouseEnter);
        el.addEventListener("mouseleave", handleMouseLeave);
      });
      return interactiveElements;
    };

    // Initial setup + re-setup on DOM changes
    let elements = addHoverListeners();
    const observer = new MutationObserver(() => {
      elements.forEach((el) => {
        el.removeEventListener("mouseenter", handleMouseEnter);
        el.removeEventListener("mouseleave", handleMouseLeave);
      });
      elements = addHoverListeners();
    });
    observer.observe(document.body, { childList: true, subtree: true });

    return () => {
      document.removeEventListener("mousemove", handleMouseMove);
      elements.forEach((el) => {
        el.removeEventListener("mouseenter", handleMouseEnter);
        el.removeEventListener("mouseleave", handleMouseLeave);
      });
      observer.disconnect();
    };
  }, []);

  if (!isVisible) return null;

  return (
    <>
      {/* Small dot */}
      <div
        className="fixed pointer-events-none z-[9999] mix-blend-difference"
        style={{
          left: position.x - 4,
          top: position.y - 4,
          width: 8,
          height: 8,
          borderRadius: "50%",
          backgroundColor: "white",
          transition: "transform 0.1s",
          transform: isHovering ? "scale(2)" : "scale(1)",
        }}
      />
      {/* Outer ring */}
      <div
        className="fixed pointer-events-none z-[9999] mix-blend-difference"
        style={{
          left: position.x - 16,
          top: position.y - 16,
          width: 32,
          height: 32,
          borderRadius: "50%",
          border: "1px solid rgba(255,255,255,0.5)",
          transition: "transform 0.15s ease-out",
          transform: isHovering ? "scale(1.5)" : "scale(1)",
        }}
      />
    </>
  );
}
```

**Step 6: Create SmoothScroll wrapper**

Create `components/layout/SmoothScroll.tsx`:

```tsx
"use client";

import { useEffect } from "react";
import Lenis from "lenis";

export default function SmoothScroll({
  children,
}: {
  children: React.ReactNode;
}) {
  useEffect(() => {
    const lenis = new Lenis({
      duration: 1.2,
      easing: (t: number) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
      touchMultiplier: 2,
    });

    function raf(time: number) {
      lenis.raf(time);
      requestAnimationFrame(raf);
    }
    requestAnimationFrame(raf);

    return () => lenis.destroy();
  }, []);

  return <>{children}</>;
}
```

**Step 7: Create template.tsx for page transitions**

Create `app/template.tsx`:

```tsx
"use client";

import { AnimatePresence, motion } from "framer-motion";
import { usePathname } from "next/navigation";

export default function Template({
  children,
}: {
  children: React.ReactNode;
}) {
  const pathname = usePathname();

  return (
    <AnimatePresence mode="wait">
      <motion.div
        key={pathname}
        initial={{ opacity: 0, y: 20 }}
        animate={{ opacity: 1, y: 0 }}
        exit={{ opacity: 0, y: -20 }}
        transition={{ duration: 0.3, ease: "easeInOut" }}
      >
        {children}
      </motion.div>
    </AnimatePresence>
  );
}
```

**Step 8: Update layout.tsx**

Read the current `app/layout.tsx` and update it to:
1. Import `LazyMotion` and `domAnimation` from `framer-motion`
2. Import `SmoothScroll` from `@/components/layout/SmoothScroll`
3. Import `CustomCursor` from `@/components/cursor/CustomCursor`
4. Wrap children with `<LazyMotion features={domAnimation}>`, `<SmoothScroll>`, and render `<CustomCursor />`

Updated `app/layout.tsx` should look like:

```tsx
import type { Metadata } from "next";
import { Outfit } from "next/font/google";
import { LazyMotion, domAnimation } from "framer-motion";
import "./globals.css";
import SmoothScroll from "@/components/layout/SmoothScroll";
import CustomCursor from "@/components/cursor/CustomCursor";

const outfit = Outfit({
  variable: "--font-outfit",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800"],
});

export const metadata: Metadata = {
  title: "Kazamiyuuji | Portfolio",
  description: "Frutiger cyber aesthetic portfolio — clean, organic, futuristic",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${outfit.variable} antialiased`}>
      <body className="min-h-screen font-[family-name:var(--font-body)]">
        <CustomCursor />
        <SmoothScroll>
          <LazyMotion features={domAnimation}>
            {children}
          </LazyMotion>
        </SmoothScroll>
      </body>
    </html>
  );
}
```

**Step 9: Add skeleton shimmer animation to globals.css**

Read `app/globals.css` and append the skeleton shimmer keyframes and class:

```css
/* ---------------------------------------------------------------
   Skeleton Loading Animation
   --------------------------------------------------------------- */
.skeleton {
  background: linear-gradient(
    90deg,
    rgba(255, 255, 255, 0.05) 25%,
    rgba(255, 255, 255, 0.1) 50%,
    rgba(255, 255, 255, 0.05) 75%
  );
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite;
  border-radius: 8px;
}

@keyframes shimmer {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

/* Hide custom cursor on touch devices */
@media (pointer: coarse) {
  .custom-cursor { display: none !important; }
}
```

**Step 10: Verify build**

```bash
cd D:/goat && npm run build 2>&1 | grep -i "error" || echo "Build succeeded"
```

**Step 11: Commit**

```bash
git add -A
git commit -m "feat(animations): add Framer Motion transitions, Lenis smooth scroll, custom cursor"
```
  </action>
  <verify>
    <automated>cd D:/goat && npm run build 2>&1 | grep -i "error" || echo "Build succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - ANIM-01: Page transitions with AnimatePresence in template.tsx
    - ANIM-02: HoverEffect component ready for use
    - ANIM-03: ScrollReveal component ready for use
    - ANIM-04: Lenis smooth scrolling active
    - ANIM-05: Custom cursor with mix-blend-mode
    - ANIM-06: LoadingSkeleton with shimmer animation
    - PERF-02: LazyMotion with domAnimation in layout
  </acceptance_criteria>
</task>

<task type="quick">
  <name>Task 3: Enrich Page Content and Apply Animations</name>
  <files>
    lib/data/portfolio.ts,
    app/page.tsx,
    app/about/page.tsx,
    app/gallery/page.tsx,
    app/contact/page.tsx
  </files>
  <action>
Per PAGE-01 through PAGE-05, CONTENT-01, CONTENT-02:

**Step 1: Update portfolio data**

Read `lib/data/portfolio.ts` and replace its content with enriched version:

```ts
/**
 * Portfolio content data
 * Centralized content for all pages — easy to update without touching components.
 */

export const siteMetadata = {
  title: "Kazamiyuuji | Portfolio",
  description: "Frutiger cyber aesthetic portfolio — clean, organic, futuristic",
  author: "Kazamiyuuji",
};

export const navigationLinks = [
  { href: "/", label: "Home" },
  { href: "/about", label: "About" },
  { href: "/gallery", label: "Gallery" },
  { href: "/contact", label: "Contact" },
] as const;

export const heroContent = {
  name: "Kazamiyuuji",
  tagline: "Frutiger Cyber Portfolio",
  description:
    "Crafting digital experiences where organic shapes meet futuristic aesthetics. A blend of nature-inspired design and cutting-edge technology.",
  cta: {
    primary: { label: "About Me", href: "/about" },
    secondary: { label: "View Work", href: "/gallery" },
  },
} as const;

export const aboutContent = {
  title: "About Me",
  description:
    "I'm a creative developer passionate about building interfaces that feel alive — where clean gradients, organic motion, and thoughtful interaction design come together.",
  sections: [
    {
      heading: "Philosophy",
      body: "I believe in design that breathes. Every pixel should feel intentional, every animation purposeful. My work draws from the Frutiger aesthetic — that optimistic, nature-meets-technology visual language that makes digital spaces feel warm and inviting.",
    },
    {
      heading: "Skills",
      items: [
        "Frontend Development",
        "UI/UX Design",
        "Creative Coding",
        "3D Visualization",
        "Motion Design",
        "Responsive Design",
      ],
    },
    {
      heading: "Tools",
      items: [
        "Next.js",
        "TypeScript",
        "Tailwind CSS",
        "Framer Motion",
        "Three.js",
        "Figma",
      ],
    },
  ],
} as const;

export interface GalleryItem {
  id: number;
  title: string;
  description: string;
  image: string;
  tags: string[];
}

export const galleryContent = {
  title: "Gallery",
  description: "A curated collection of projects and creative experiments.",
  items: [
    {
      id: 1,
      title: "Aurora Dashboard",
      description: "Real-time data visualization with organic glassmorphism panels",
      image: "https://picsum.photos/seed/aurora/600/400",
      tags: ["React", "D3.js", "Glassmorphism"],
    },
    {
      id: 2,
      title: "Nebula Commerce",
      description: "E-commerce concept with 3D product previews and smooth transitions",
      image: "https://picsum.photos/seed/nebula/600/400",
      tags: ["Next.js", "Three.js", "Stripe"],
    },
    {
      id: 3,
      title: "Prism Analytics",
      description: "Analytics dashboard with nature-inspired data storytelling",
      image: "https://picsum.photos/seed/prism/600/400",
      tags: ["TypeScript", "Charts", "Tailwind"],
    },
    {
      id: 4,
      title: "Flux Portfolio",
      description: "Personal portfolio with scroll-driven animations and 3D elements",
      image: "https://picsum.photos/seed/flux/600/400",
      tags: ["GSAP", "R3F", "Framer Motion"],
    },
    {
      id: 5,
      title: "Terra Forms",
      description: "Interactive form builder with real-time validation and glass UI",
      image: "https://picsum.photos/seed/terra/600/400",
      tags: ["React", "Formik", "CSS Grid"],
    },
    {
      id: 6,
      title: "Solstice Mobile",
      description: "Mobile-first progressive web app with offline support",
      image: "https://picsum.photos/seed/solstice/600/400",
      tags: ["PWA", "Service Worker", "Responsive"],
    },
  ] as GalleryItem[],
} as const;

export const contactContent = {
  title: "Get in Touch",
  description:
    "Have a project in mind or just want to chat about design? I'd love to hear from you.",
  email: "hello@kazamiyuuji.dev",
  social: [
    { label: "GitHub", url: "https://github.com", icon: "github" },
    { label: "Twitter", url: "https://twitter.com", icon: "twitter" },
    { label: "LinkedIn", url: "https://linkedin.com", icon: "linkedin" },
  ],
} as const;
```

**Step 2: Update Home page**

Read `app/page.tsx` and replace with enriched version that uses ScrollReveal and HoverEffect:

```tsx
import Navigation from "@/components/layout/Navigation";
import Footer from "@/components/layout/Footer";
import GlassPanel from "@/components/glass/GlassPanel";
import Blob from "@/components/shapes/Blob";
import Wave from "@/components/shapes/Wave";
import Ellipse from "@/components/shapes/Ellipse";
import ScrollReveal from "@/components/animation/ScrollReveal";
import HoverEffect from "@/components/animation/HoverEffect";
import { heroContent } from "@/lib/data/portfolio";

export default function Home() {
  return (
    <div className="min-h-screen flex flex-col relative overflow-hidden">
      {/* Background organic shapes */}
      <Blob
        color="rgba(109, 179, 242, 0.2)"
        size={500}
        className="top-20 -left-40"
      />
      <Blob
        color="rgba(0, 206, 209, 0.15)"
        size={400}
        className="bottom-40 -right-32"
      />
      <Ellipse
        color="rgba(241, 196, 15, 0.06)"
        width={800}
        height={400}
        className="top-1/3 left-1/2 -translate-x-1/2"
      />

      <Navigation />

      <main className="flex-1 flex items-center justify-center px-6 pt-24">
        <ScrollReveal>
          <GlassPanel variant="hero" className="max-w-3xl text-center p-12">
            <h1 className="text-5xl md:text-7xl font-bold mb-6 gradient-primary bg-clip-text text-transparent">
              {heroContent.name}
            </h1>
            <p className="text-xl text-surface/80 mb-4">
              {heroContent.tagline}
            </p>
            <p className="text-base text-surface/50 mb-8 max-w-xl mx-auto">
              {heroContent.description}
            </p>
            <div className="flex gap-4 justify-center">
              <HoverEffect>
                <a
                  href={heroContent.cta.primary.href}
                  className="px-6 py-3 gradient-primary rounded-[var(--radius-button)] text-surface font-semibold hover:shadow-[var(--shadow-glow-primary)] transition-shadow inline-block"
                >
                  {heroContent.cta.primary.label}
                </a>
              </HoverEffect>
              <HoverEffect>
                <a
                  href={heroContent.cta.secondary.href}
                  className="px-6 py-3 glass-panel text-surface font-semibold hover:shadow-[var(--shadow-glow-accent)] transition-shadow inline-block"
                >
                  {heroContent.cta.secondary.label}
                </a>
              </HoverEffect>
            </div>
          </GlassPanel>
        </ScrollReveal>
      </main>

      {/* Features section */}
      <section className="py-20 px-6">
        <div className="max-w-4xl mx-auto grid grid-cols-1 md:grid-cols-3 gap-6">
          {[
            { title: "Organic Design", desc: "Nature-inspired shapes and gradients that feel alive", icon: "🌿" },
            { title: "Smooth Motion", desc: "Fluid animations that guide your eye naturally", icon: "✨" },
            { title: "Clean Code", desc: "Modern stack, optimized performance, accessible", icon: "⚡" },
          ].map((feature, i) => (
            <ScrollReveal key={feature.title} delay={i * 0.1}>
              <HoverEffect glow>
                <GlassPanel variant="card" className="p-6 text-center">
                  <div className="text-4xl mb-4">{feature.icon}</div>
                  <h3 className="text-lg font-semibold text-surface mb-2">{feature.title}</h3>
                  <p className="text-sm text-surface/50">{feature.desc}</p>
                </GlassPanel>
              </HoverEffect>
            </ScrollReveal>
          ))}
        </div>
      </section>

      <Wave color="rgba(0, 206, 209, 0.15)" className="mt-auto" />

      <Footer />
    </div>
  );
}
```

**Step 3: Update About page**

Read `app/about/page.tsx` and replace with enriched version:

```tsx
import Navigation from "@/components/layout/Navigation";
import Footer from "@/components/layout/Footer";
import GlassPanel from "@/components/glass/GlassPanel";
import Blob from "@/components/shapes/Blob";
import Ellipse from "@/components/shapes/Ellipse";
import ScrollReveal from "@/components/animation/ScrollReveal";
import HoverEffect from "@/components/animation/HoverEffect";
import { aboutContent } from "@/lib/data/portfolio";

export default function AboutPage() {
  return (
    <div className="min-h-screen flex flex-col relative overflow-hidden">
      <Blob
        color="rgba(0, 206, 209, 0.15)"
        size={400}
        className="top-32 -right-32"
      />
      <Ellipse
        color="rgba(109, 179, 242, 0.08)"
        width={700}
        height={350}
        className="bottom-20 -left-48"
      />

      <Navigation />

      <main className="flex-1 px-6 pt-28 pb-16 max-w-4xl mx-auto w-full">
        <ScrollReveal>
          <GlassPanel className="p-8 md:p-12">
            <h1 className="text-4xl md:text-5xl font-bold mb-6 gradient-primary bg-clip-text text-transparent">
              {aboutContent.title}
            </h1>
            <p className="text-lg text-surface/70 mb-10 leading-relaxed">
              {aboutContent.description}
            </p>

            <div className="space-y-10">
              {aboutContent.sections.map((section, i) => (
                <ScrollReveal key={section.heading} delay={i * 0.1}>
                  <div>
                    <h2 className="text-2xl font-semibold text-primary mb-4">
                      {section.heading}
                    </h2>
                    {"body" in section ? (
                      <p className="text-surface/60 leading-relaxed">
                        {section.body}
                      </p>
                    ) : (
                      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3">
                        {section.items.map((item, j) => (
                          <HoverEffect key={item} scale={1.03}>
                            <div className="glass-panel px-4 py-3 rounded-[var(--radius-card)] text-surface/80 font-medium text-sm">
                              {item}
                            </div>
                          </HoverEffect>
                        ))}
                      </div>
                    )}
                  </div>
                </ScrollReveal>
              ))}
            </div>
          </GlassPanel>
        </ScrollReveal>
      </main>

      <Footer />
    </div>
  );
}
```

**Step 4: Update Gallery page**

Read `app/gallery/page.tsx` and replace with version that uses real gallery data:

```tsx
import Navigation from "@/components/layout/Navigation";
import Footer from "@/components/layout/Footer";
import GlassPanel from "@/components/glass/GlassPanel";
import ScrollReveal from "@/components/animation/ScrollReveal";
import HoverEffect from "@/components/animation/HoverEffect";
import { galleryContent } from "@/lib/data/portfolio";

export default function GalleryPage() {
  return (
    <div className="min-h-screen flex flex-col relative overflow-hidden">
      <Navigation />

      <main className="flex-1 px-6 pt-28 pb-16 max-w-6xl mx-auto w-full">
        <ScrollReveal>
          <h1 className="text-4xl md:text-5xl font-bold mb-4 gradient-primary bg-clip-text text-transparent">
            {galleryContent.title}
          </h1>
          <p className="text-lg text-surface/70 mb-12">
            {galleryContent.description}
          </p>
        </ScrollReveal>

        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {galleryContent.items.map((item, i) => (
            <ScrollReveal key={item.id} delay={i * 0.08}>
              <HoverEffect glow>
                <GlassPanel variant="card" className="overflow-hidden group">
                  <div className="aspect-video overflow-hidden">
                    <img
                      src={item.image}
                      alt={item.title}
                      className="w-full h-full object-cover transition-transform duration-500 group-hover:scale-110"
                      loading="lazy"
                    />
                  </div>
                  <div className="p-5">
                    <h3 className="text-lg font-semibold text-surface mb-2">
                      {item.title}
                    </h3>
                    <p className="text-sm text-surface/50 mb-3">
                      {item.description}
                    </p>
                    <div className="flex flex-wrap gap-2">
                      {item.tags.map((tag) => (
                        <span
                          key={tag}
                          className="text-xs px-2 py-1 rounded-full bg-primary/10 text-primary"
                        >
                          {tag}
                        </span>
                      ))}
                    </div>
                  </div>
                </GlassPanel>
              </HoverEffect>
            </ScrollReveal>
          ))}
        </div>
      </main>

      <Footer />
    </div>
  );
}
```

**Step 5: Update Contact page**

Read `app/contact/page.tsx` and replace with enriched version that uses ScrollReveal:

```tsx
"use client";

import Navigation from "@/components/layout/Navigation";
import Footer from "@/components/layout/Footer";
import GlassPanel from "@/components/glass/GlassPanel";
import Blob from "@/components/shapes/Blob";
import Ellipse from "@/components/shapes/Ellipse";
import ScrollReveal from "@/components/animation/ScrollReveal";
import HoverEffect from "@/components/animation/HoverEffect";
import { contactContent } from "@/lib/data/portfolio";

export default function ContactPage() {
  return (
    <div className="min-h-screen flex flex-col relative overflow-hidden">
      <Blob
        color="rgba(46, 204, 113, 0.15)"
        size={350}
        className="top-24 -left-32"
      />
      <Ellipse
        color="rgba(241, 196, 15, 0.08)"
        width={500}
        height={250}
        className="bottom-32 -right-24"
      />

      <Navigation />

      <main className="flex-1 flex items-center justify-center px-6 pt-24 pb-16">
        <ScrollReveal>
          <GlassPanel variant="hero" className="max-w-xl w-full p-8 md:p-12">
            <h1 className="text-4xl md:text-5xl font-bold mb-4 gradient-primary bg-clip-text text-transparent text-center">
              {contactContent.title}
            </h1>
            <p className="text-lg text-surface/70 mb-10 text-center">
              {contactContent.description}
            </p>

            <form className="space-y-5" onSubmit={(e) => e.preventDefault()}>
              <div>
                <label
                  htmlFor="name"
                  className="block text-sm font-medium text-surface/60 mb-1.5"
                >
                  Name
                </label>
                <input
                  id="name"
                  type="text"
                  className="w-full px-4 py-3 rounded-[var(--radius-button)] bg-white/5 border border-white/10 text-surface placeholder:text-surface/30 focus:outline-none focus:border-accent transition-colors"
                  placeholder="Your name"
                />
              </div>
              <div>
                <label
                  htmlFor="email"
                  className="block text-sm font-medium text-surface/60 mb-1.5"
                >
                  Email
                </label>
                <input
                  id="email"
                  type="email"
                  className="w-full px-4 py-3 rounded-[var(--radius-button)] bg-white/5 border border-white/10 text-surface placeholder:text-surface/30 focus:outline-none focus:border-accent transition-colors"
                  placeholder="you@example.com"
                />
              </div>
              <div>
                <label
                  htmlFor="message"
                  className="block text-sm font-medium text-surface/60 mb-1.5"
                >
                  Message
                </label>
                <textarea
                  id="message"
                  rows={4}
                  className="w-full px-4 py-3 rounded-[var(--radius-button)] bg-white/5 border border-white/10 text-surface placeholder:text-surface/30 focus:outline-none focus:border-accent transition-colors resize-none"
                  placeholder="Tell me about your project..."
                />
              </div>
              <HoverEffect>
                <button
                  type="submit"
                  className="w-full py-3 gradient-primary rounded-[var(--radius-button)] text-surface font-semibold hover:shadow-[var(--shadow-glow-primary)] transition-shadow"
                >
                  Send Message
                </button>
              </HoverEffect>
            </form>

            <div className="mt-8 pt-6 border-t border-white/5">
              <p className="text-center text-surface/40 text-sm mb-4">
                Or reach out directly
              </p>
              <div className="flex justify-center gap-4">
                {contactContent.social.map((link) => (
                  <HoverEffect key={link.label} scale={1.1}>
                    <a
                      href={link.url}
                      target="_blank"
                      rel="noopener noreferrer"
                      className="glass-panel px-4 py-2 rounded-[var(--radius-button)] text-surface/60 hover:text-surface text-sm font-medium transition-colors"
                    >
                      {link.label}
                    </a>
                  </HoverEffect>
                ))}
              </div>
            </div>
          </GlassPanel>
        </ScrollReveal>
      </main>

      <Footer />
    </div>
  );
}
```

**Step 6: Verify build**

```bash
cd D:/goat && npm run build 2>&1 | grep -i "error" || echo "Build succeeded"
```

**Step 7: Commit**

```bash
git add -A
git commit -m "feat(content): enrich all pages with content, images, and animations"
```
  </action>
  <verify>
    <automated>cd D:/goat && npm run build 2>&1 | grep -i "error" || echo "Build succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - PAGE-01: Home page has hero, description, feature cards
    - PAGE-02: About page has bio, philosophy, skills, tools
    - PAGE-03: Gallery page has 6 project cards with images
    - PAGE-04: Contact page has form and social links
    - PAGE-05: Navigation responsive (already from Phase 1)
    - CONTENT-01: Placeholder images from picsum.photos
    - CONTENT-02: Rich placeholder text content
  </acceptance_criteria>
</task>

<task type="quick">
  <name>Task 4: Verify and Commit Final State</name>
  <files>
    package.json
  </files>
  <action>
Per all Phase 2 requirements:

**Step 1: Run full build**

```bash
cd D:/goat && npm run build
```

**Step 2: Verify output**

```bash
ls out/
```

Should contain:
- index.html
- about/index.html
- gallery/index.html
- contact/index.html

**Step 3: Verify all source files exist**

```bash
find app/ components/ lib/ -name "*.tsx" -o -name "*.ts" -o -name "*.css" | sort
```

**Step 4: Final commit (if any uncommitted changes)**

```bash
git status
# If clean, no commit needed
# If dirty:
git add -A && git commit -m "feat(phase-2): complete content pages and animations"
```
  </action>
  <verify>
    <automated>cd D:/goat && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All requirements PASS
    - Build succeeds
    - Static export generated
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install | External packages fetched from registry — framer-motion and lenis verified |
| picsum.photos | External image CDN — images loaded at runtime |
| lenis | Smooth scroll library — client-side only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering | npm packages | high | mitigate | framer-motion and lenis are well-maintained, verified packages |
| T-2-02 | Information Disclosure | picsum.photos | low | accept | Images are public, no user data sent |
| T-2-03 | Denial of Service | picsum.photos | low | accept | Images are optional — page renders without them |
</threat_model>

<verification>
## Overall Phase Verification

1. **Build succeeds:** `npm run build` completes without errors
2. **Page transitions:** Navigate between pages — fade+slide animation fires
3. **Scroll reveals:** Scroll down on Home/About — elements animate into view
4. **Hover effects:** Hover over cards/buttons — scale+glow effect fires
5. **Smooth scrolling:** Scroll with Lenis — smooth, eased scrolling
6. **Custom cursor:** Move mouse on desktop — dot+ring follows with blend mode
7. **Loading skeletons:** Skeleton shimmer animation visible
8. **Gallery images:** 6 project cards with picsum.photos images
9. **Static export:** out/ directory contains all HTML files
</verification>

<success_criteria>
- [ ] framer-motion and lenis in package.json
- [ ] LazyMotion with domAnimation in layout.tsx
- [ ] template.tsx with AnimatePresence page transitions
- [ ] SmoothScroll wrapping app body
- [ ] CustomCursor rendering with mix-blend-mode
- [ ] ScrollReveal and HoverEffect components created
- [ ] LoadingSkeleton with shimmer animation
- [ ] All 4 pages with enriched content
- [ ] Gallery with 6 project cards and images
- [ ] Contact form with social links
- [ ] npm run build completes successfully
- [ ] out/ directory contains all HTML pages
- [ ] Git commits created with correct prefixes
</success_criteria>

<output>
Create `.planning/phases/02-content-pages-animations/02-01-SUMMARY.md` when done
</output>
